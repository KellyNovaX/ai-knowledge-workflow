# 0.3.3 验证记录

## 文件树双击默认程序打开（未发布）

2026-09-12，文件树双击复用右键菜单的系统默认程序打开逻辑。仅监听文件树的双击事件，跳过文件夹和正在编辑的文件名；布局变化时补充监听，已绑定容器不重复注册，插件卸载时由 `registerDomEvent` 清理。

- `npm run build` 通过，包含 TypeScript 类型检查并生成生产 `main.js`。
- `npm run lint` 为 0 错误、1 条已有设置搜索接口建议；`git diff --check` 通过。
- 按项目要求未新增测试用例。真实 Obsidian UI 尚未验收；需在更新插件后检查 Markdown/TXT/PDF 双击打开、单击预览、目录展开收起、重命名时双击选词及停用插件后的行为。

## 0.3.3 技能随包增量验证

2026-09-05，本次将原有三项通用技能作为固定文本资源打进插件。初始化预览列出技能目的和目标目录，用户确认后补齐当前知识库中的缺失文件；不下载、不执行、不自动更新技能。源码仓库未新增测试用例文件。

- 临时真实目录适配器验证：空库预览 69 文件、31 目录且零写入；初始化生成 52 个基础文件和 17 个技能文件。技能共 69182 字节，与 `companion-skills/` 原文件逐字节一致。
- 重复初始化零写入；已有 52 个基础文件的旧库仅补齐 17 个技能文件，原文件内容全部保留；自定义技能文件也保留。
- 父目录被普通文件占用、目标文件被目录占用时均在写入前阻断；模拟部分写入失败后，重跑只补剩余 13 文件，已写入的 56 文件保持不变。
- 实际初始化入口的临时替身验证覆盖已齐全、取消、成功、预览失败、写入失败和校验失败六种情况；取消时未调用写入，校验失败与写入失败分别提示。
- 全部 51 个 TypeScript 输入（含文本资源声明）通过当前类型检查和官方 1.4.11 声明兼容性检查；17 个文本资源均被构建配置覆盖。官方 lint 为 0 错误，保留 1 条新版设置搜索接口建议。
- 仅复制生产 `main.js`、`manifest.json` 和 `styles.css` 到隔离目录，以 Obsidian/Electron 替身加载实际插件入口，确认初始化仍生成全部 69 文件、17 技能字节一致；没有运行时技能文件导入，也未调用 Python、CLI 或外部应用。该场景不依赖源码仓库中的技能目录。
- 生产构建和打包通过：插件 ZIP 为 11 个文件，Starter ZIP 为 81 个文件；Starter 由初始化器生成 69 文件后加入插件和入口说明，17 个技能文件逐字节核对通过。源码候选 87 文件及发布资产的隐私模式检查未发现个人或公司标识、私有地址或凭据；ZIP 不含运行配置或知识库历史。
- 上述验证不代表真实 Obsidian 窗口验收，也不代表 0.3.3 社区扫描已通过。Python、AI 助手和可选 CLI 仍需用户自行准备；已有技能文件不会随插件升级自动更新。

## 0.3.2 审查修复验证

2026-09-05，根据社区自动审查修复卸载时移除页签、最低版本 API、设置标题及直接样式赋值问题；最低 Obsidian 版本保持 1.5.0。

- 全部 49 个源码模块通过当前及官方 1.4.11 声明的类型检查；新版页签显示接口由 1.7.2 版本判断保护，旧版回退到激活页签。折叠侧栏中的旧版页签显示未做实机验证。
- 全部源码纳入类型检查；官方 Obsidian ESLint 规则检查为 0 错误、1 条建议。保留的建议是新版声明式设置搜索接口，现有设置页仍可使用。
- 界面改为公共内容容器和创建元素快捷方法；修正回调绑定、异步调用、计时器及 UI 大小写。静态样式移入 CSS，去除 !important。
- 已验证设置和任务编号的非法输入处理，保留同一配置文件中的内部数据；检查状态转换、特殊 Markdown 链接、FAQ 旧路径及桌面应用接口的替身行为。
- 英文 README 与完整中文说明同时提供；文件系统、CLI、剪贴板及外部服务能力继续披露。分享 ZIP 和校验文件是有意保留的附加资产。
- 生产构建与打包通过：插件 ZIP 11 个文件，空白知识库 ZIP 81 个文件（其中初始化模板 52 个）。
- 依赖安装禁用安装脚本；构建依赖已更新，npm audit 检查未报告已知漏洞。
- 上述检查不代表社区审查最终通过，也不代表真实 Obsidian UI、Windows/Linux 或外部 CLI 服务实机验收。

## 0.3.1 增量验证

2026-09-05，本补丁仅调整已有终端页的选择方式，不再读取 `WorkspaceLeaf.parent`；最低 Obsidian 版本保持 1.5.0。

- 使用 1.5.0 发布前的[官方 1.4.11 类型定义](https://github.com/obsidianmd/obsidian-api/blob/791214a68d0dc322b88e5abce617bdf603cc2a2d/obsidian.d.ts)检查全部 44 个源码模块，0 个错误。
- 终端页选择通过临时函数验证：已有终端时激活首个结果，无终端时不操作，不访问父级分组字段。
- 当前类型检查、生产构建及打包通过；源码和分发内容已作隐私差异检查。
- 这些结果不代表真实 Obsidian 1.5.0 或桌面 UI 验收；第三方终端和外部 CLI 服务仍需实际环境验证。

## 原功能验证（0.3.0）

2026-09-05，以下记录来自 0.3.0 的实际函数与临时文件适配器验证；本补丁未重跑全部原功能场景，没有在源码仓库新增测试用例文件。

- 空库初始化：52 个文件、19 个目录，校验 0 问题；再次执行不覆盖已有内容。
- 无项目创建通用任务；有项目创建项目任务和 feature、incident、datafix、data-export 四类工作流。
- 共 8 个任务，创建、完成、重复完成、重开及完成时间语义通过。
- 检查 134 条本地链接，0 失效；工作流入口约 1.1–1.3 KB。
- 批量任务编号、含引号/美元符号/true/null 的标题、空格括号路径、自定义配置目录通过。
- Obsidian 的默认 Wiki 链接偏好保持不变，新任务生成标准 Markdown。
- 配套任务脚本检查 0 错误、0 维护警告；新任务的 5 条目标分支待填写提醒符合预期。
- 类型检查、生产构建通过；插件安装包与空白知识库包的结构、文档链接和 SHA256 校验通过。同输入重复打包的校验值一致。
- Codex/自定义 CLI 命令参数、工作目录、提示词标准输入及失败处理通过本地替身验证，没有发起真实 AI 请求。
- 周报本地日期、周一/周日和跨年 ISO 周编号通过临时验证。

边界：适配器替代了 Obsidian 的文件与元数据接口，YAML 使用标准解析实现。尚未完成真实 Obsidian UI、Windows/Linux 及外部 AI CLI 服务的完整验收。以上结果不代表社区目录审核通过。

上传前隐私核查覆盖源码候选、两个 ZIP 和三个单独发布文件；公开作者使用 GitHub 昵称，分发包不含本地用户名、邮箱、真实业务资料、个人配置、凭据、源码映射或原仓库历史。
